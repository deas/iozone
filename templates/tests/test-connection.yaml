apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iozone.fullname" . }}-test-connection"
  labels:
{{ include "iozone.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "iozone.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
